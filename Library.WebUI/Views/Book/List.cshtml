@model Library.WebUI.Models.BookListViewModel

@{
    ViewBag.Title = "Books";
}

@foreach (var b in Model.Books)
{  
        Html.RenderPartial("BookSummary", b);  
}    
    <div class="pages">
        @Html.PageLinks(Model.PagingInfo, x => Url.Action("List", new { page = x}))
    </div>

